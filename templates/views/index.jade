extends ../layouts/default

block content
	div(style="padding-left: 5vw; padding-right: 5vw;")

	.row
		.panel.panel-default
			.panel-heading
				h3.panel-title: a(href='/keystone/signin') Log Into Hope For The Young Filesystem
			.panel-body
				.col-10
				.col-80
					p     Welcom to HftY database portal,please sign in below
					if user
						a(href='/keystone').btn.btn-default Got to Admin
						a(href='/keystone/signout').btn.btn-link Sign out
					else
						a(href='/keystone/signin').btn.btn-default Sign in
			
	
